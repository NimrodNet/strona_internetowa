from lokalizacja_elementu import *

class Element_mapy_strony:

    def __init__(self, link):
        
    def konstruuj(self):
        poczatek = self.poczatek()
        poczatek += "\n"
        koniec = self.koniec()
        

    def poczatek(cls):
        return "<url>"

    def koniec(cls):
        return "</url>"
